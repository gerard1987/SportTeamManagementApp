<UserControl
    x:Class="SportTeamManagementApp.Results"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SportTeamManagementApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local1="using:SportTeamManagementApp.Data.Entities"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel>
            <ListBox x:Name="MatchesListBox">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <TextBlock Text="{Binding MatchId}" Width="50"/>
                            <TextBlock Text="{Binding HomeTeamName}" Width="200"/>
                            <TextBlock Text="{Binding HomeTeamScore}" Width="50"/>
                            <TextBlock Text="vs" Width="30"/>
                            <TextBlock Text="{Binding AwayTeamScore}" Width="50"/>
                            <TextBlock Text="{Binding AwayTeamName}" Width="200"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
